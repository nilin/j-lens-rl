# V8-local RTX 4090 handoff

V8-local is a fresh whole replication of the frozen V7 profanity experiment on seeds 200–207. It changes only the backend (one local RTX 4090), tracking transport (offline W&B), seeds, and isolated state. The reward is solely the intrinsic J-lens score for the emotionally charged words `damn` and `fuck`; correctness is never a training reward. The sign-flip controls change only the sole component weight from -1 to +1.

The exact 400-row curve and this lineage are already exposed development data. The 0/4/10/20 eight-seed curve is only an operational consistency gate. It is not independent or inferential evidence. Only the conditionally still-unopened 900-row final can support the registered crossed-bootstrap/sign-test claim.

The launch-enabled amendment now pins the pushed V7 pre-recovery closeout,
post-recovery authoritative wrapper, exact lease-retirement receipt, and all 12
source-evidence files. It proves the failed V7 app stopped, the shared lease was
retired, and no final unlock, collection, evaluation, comparison, or outcome
access occurred. Commit and push all V8 bytes first; preparation is permitted
only from a clean checkout whose `HEAD` equals `origin/main`.

Preparation creates a detached clean runtime worktree at the registered commit. Every subprocess runs there with `PYTHONPATH` and `JLENS_REPOSITORY_ROOT` pinned to it, while state remains under ignored `.confirmatory/v8_local`. The runner requires the exact registered RTX 4090 UUID/name/driver/memory and `.venv` torch/CUDA stack, takes a nonblocking OS lock for the whole attempt, and writes an fsynced dispatch intent before each serial GPU subprocess.

Each training run has its own absolute offline-W&B root. A run is complete only after W&B finishes and `wandb_offline_terminal_receipt.json` binds the unique syncable offline-run root, all seven terminal evidence files, and its full file/symlink inventory. Never resume or rerun optimization. A fully complete run may only have a missing orchestration receipt reconstructed; partial/nonterminal output closes the whole attempt and requires a fresh registration/state/claim/all-runs attempt.

Run order is fixed: eight treatments ascending, curve decision once, eight controls ascending only if the curve passes, then unlock. `run-treatments` is a diagnostic phase boundary; `run-training` runs through controls and unlock. The sealed final is not automated and must not be opened until its evaluation/analysis runner and tests are completed and independently audited. Any partial or uncertain sealed collection spends that allocation and fails closed.
